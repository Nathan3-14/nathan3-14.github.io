name: Demo
run-name: ${{ github.actor }} demo
on: [push]
jobs:
    Convert yaml:
        runs-on: ubuntu-latest
        steps:
            - name: Check out repo
              uses: actions/checkout@v4
            - name: Get convert.py
            - run: mkdir temp
            - run: cd temp
            - run: touch test.file
            - run: ls